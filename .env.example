# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings (currently using SQLite, no credentials needed)
# Add database credentials here when moving to PostgreSQL in production
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Email settings (currently using console backend for development)
# Add email credentials here when configuring real email in production
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-password
# EMAIL_USE_TLS=True
